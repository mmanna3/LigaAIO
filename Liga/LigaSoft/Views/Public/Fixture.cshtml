@using LigaSoft.ExtensionMethods
@using LigaSoft.Models.Enums
@model LigaSoft.Models.ViewModels.PublicIndexVM

		<h1>@Model.Fixture.Titulo</h1>

		@foreach (var fecha in Model.Fixture.Fechas)
		{
			<div class="container pull-left">
				<h3>@fecha.Titulo</h3>
				<table class="table tabla-posiciones">
					<thead>
						<tr>
							<th>
								@fecha.Titulo
							</th>
							<th>
							</th>
							<th>
							</th>
							<th>
							</th>
							<th>
								@fecha.DiaDeLaFecha
							</th>
						</tr>
					</thead>
					<tbody>
					@foreach (var renglon in fecha.LocalVisitante)
					{
					<tr>
						<td class="escudo"><img width="30px" heigh="auto" src=@renglon.EscudoLocal alt="Escudo" /></td>
						<td>							
							@renglon.Local							
						</td>
						<td>
							vs.
						</td>						
						<td>
							@renglon.Visitante
						</td>
						<td class="escudo"><img width="30px" heigh="auto" src=@renglon.EscudoVisitante alt="Escudo" /></td>
					</tr>
					}
					</tbody>
				</table>
			</div>
		}

